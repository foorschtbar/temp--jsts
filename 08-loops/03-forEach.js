'use strict';

const primes = [ 2, 3, 5, 7, 11 ];

// Array iterieren, ohne Index, aber direkt mit Element
primes.forEach(prime => {
  // ...
  // ACHTUNG: break und continue funktionieren hier nicht!
});
