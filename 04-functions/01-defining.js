'use strict';

// Function expression
const add = function (x, y) {
  return x + y;
};

// Function statement
function subtract (x, y) {
  return x - y;
}

console.log(add(23, 42));
console.log(subtract(23, 42));
